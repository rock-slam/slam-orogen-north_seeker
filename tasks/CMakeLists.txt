# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(north_seekerTaskLib)
ADD_LIBRARY(${NORTH_SEEKER_TASKLIB_NAME} SHARED 
    ${NORTH_SEEKER_TASKLIB_SOURCES})
add_dependencies(${NORTH_SEEKER_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${NORTH_SEEKER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${NORTH_SEEKER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${NORTH_SEEKER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${NORTH_SEEKER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${NORTH_SEEKER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${NORTH_SEEKER_TASKLIB_HEADERS}
    DESTINATION include/orocos/north_seeker)

